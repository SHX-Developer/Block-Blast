# Block-Blast

## Локальный запуск

Frontend:

```powershell
npm install
npm run dev
```

Обычно Vite открывается на `http://localhost:5173`.

Telegram-бот:

```powershell
cd bot
npm install
npm run dev
```

Перед запуском бота заполни нужные переменные окружения, например токен Telegram-бота.
